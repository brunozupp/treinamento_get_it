import 'package:flutter/material.dart';

class PostRegisterPage extends StatelessWidget {
  const PostRegisterPage({ Key? key }) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container(
      
    );
  }
}